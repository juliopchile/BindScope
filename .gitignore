# Optional local overrides for `make run` / `make serve` (PORT, HOST)
.env
.env.local

# Dependencies and build output (not used yet; here so they never get committed
# by accident once a stack is chosen)
node_modules/
dist/
build/
.vite/

# Logs and caches
*.log
npm-debug.log*
.npm/
.cache/

# Editor and OS noise
.DS_Store
.idea/
*.swp

# Playwright
app/test-results/
app/playwright-report/
app/blob-report/
app/playwright/.cache/
